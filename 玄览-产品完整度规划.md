# 玄览 · 产品完整度规划（PM 视角）

> 阶段：MVP 已跑通（8 体系并排推演）→ 本轮补齐「解释层」→ 下一步按优先级扩张。
> 铁律：**内核严肃（确定性算法，可快照可复现）、外壳科普（白话+诚实边界）**；**AI 只做解读，永不参与计算**。

## 一、本轮已交付（解释层 · 把"结果"翻译成"人话"）

| 模块 | 作用 | 性质 |
|---|---|---|
| 解释层 `interpretation.ts` | 8 体系各自盘面 → 总览/关键指标/大运流年时间轴 | 内核确定性，可测试 |
| 共识度 `consensus.ts` | 6 决策轴加权均值 + 一致率 + 分歧体系 | 内核确定性 |
| 综合报告 `report.ts` + `SynthesisReport.tsx` | 把八家结论汇成一篇可一口气读完的人话长文 + 一键复制 | 内核确定性 |
| 术语词典 `glossary.ts` + 点开弹层 | 10 个硬核术语白话解释 | 前端科普 |
| 共识仪表盘 `ConsensusDashboard.tsx` | 6 轴可视化，加权均值标记 + 分歧高亮 | 前端可视化 |

**阅读顺序（结果墙）：** 共识仪表盘 → 综合解读报告 → 各体系卡（卡内含该体系解读）。

## 二、竞品差距（问真八字 / 灵机 / 测测）

- **问真八字**强在：大运逐柱、流年、用神、五行旺衰、十神、神煞、古籍智能匹配。**我们已用 `interpretBazi` 覆盖同类信息**，且多体系交叉是竞品没有的。
- **灵机/测测**强在：娱乐化长文解读 + 保存/分享。**我们"综合报告"补齐了长文，但缺保存/分享闭环**（见 P0）。
- **我们的独有卖点**：跨体系一致性（共识度）——这是单体系 App 做不了的，应作为主菜而非配菜。

## 三、完整度优先级路线图

### P0（完整性缺口，最该先做）
1. **历史记录 + 导出/分享**：localStorage 存最近 N 次排盘；报告一键导出 PNG/Markdown；可生成只读分享链接。竞品标配，用户排完没处存是当前最大断点。
2. **PWA**：`manifest.json` + service worker（已规划方向），离线可看已排盘。微信/应用商店禁算命类目，PWA 是合规分发抓手。

### P1（体验与深度）
3. **AI 自然语言解读开关**：用户填问题后，可选调用自有 OpenAI 兼容 Key（走后端 SSE 代理），把确定性结构翻译成更口语的长文。**内核零改动，只在下游**——严守铁律。
4. **术语词典扩到 30+**：当前 10 个，补十神全名、神煞、宫位、分宫制等。
5. **复盘/命中率**：用户回看历史问事并标记"准/不准"——仅本人可见，平台不聚合不宣称准确率（已在 types 预留 `Outcome`）。

### P2（扩张）
6. **二期体系**：六爻、太乙、七政四余、姓名学、风水（已在 registry 预留位）。
7. **合盘/择日对比**：两人八字合婚、两个时辰奇门择日——聚合平台的"对比"价值。
8. **主题化引导**：按事业/感情/健康给不同的"怎么读这份报告"提示。

## 四、验证纪律（不重蹈"光建设不验证"）

- 任何算法改动先跑 `npm test`（golden 复现 vs 权威库），再动前端。
- 新增体系先写对照测试（如八字↔lunar、六壬↔liuren-ts），一致率不达标不入主线。
- 解释层文案改动需人工抽检诚实边界，禁止出现"必、一定、定能"等绝对化断言。

## 五、当前验证状态

- 内核测试：**30/30 通过**；`tsc --noEmit`：**0 错误**；`vite build`：**612 模块通过**。
- 诚实边界已固化：星盘 Placidus 不可切、紫微四化飞星不支持、梅花仅时间卦——UI 已置灰说明。
